To run the program, it has to be executed in a Linux environment.

Open the terminal and locate yourself in the folder in which the "Fileread.cpp" file is located, and run "make all"
Another option is to execute the "run.sh" bashscript, just type "bash run.sh" into the terminal.